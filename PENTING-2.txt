gcloud beta compute ssh --zone "us-central1-b" "instance-1"  --project "qwiklabs-gcp-01-58e79af7bc53"

rizalfathurr1999@gmail.com
deinjo99.

RUN apt-get update && apt-get install -y python3 python3-pip && apt-get install -y python3-venv && python3 -m venv ./venv && python3 -m pip install --no-cache --upgrade pip && pip3 install --no-cache rasa==1.5.3

sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt-get update

sudo apt install python3.7
sudo update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.6 1
sudo update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.7 2
sudo update-alternatives --config python3

sudo apt install python3-pip

sudo apt-get install -y python3-venv
python3 -m venv ./venv
source ./venv/bin/activate

pip3 install -U pip
pip3 install rasa
pip3 install rasa[full]

git clone https://github.com/rizal1919/Capstone_Project_B21-CAP0308.git

#heroku
sudo snap install --classic heroku

#docker
sudo apt-get update
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add
sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
sudo apt-get update
apt-cache policy docker-ce
sudo apt-get install -y docker-ce
sudo systemctl status docker

git init
git add .
git config --global user.email "rizalfathurr1999@gmail.com"
git config --global user.name "rizal"
git commit -m "hm"

FROM ubuntu:18.04
FROM rasa/rasa:latest-full
FROM rasa/rasa-sdk:latest

ENTRYPOINT []
USER root
RUN apt-get update && apt-get install -y python3 python3-pip && python3 -m pip install --no-cache --upgrade pip && pip3 install --no-cache rasa==2.7.0-full
ADD . /app/
RUN chmod +x /app/start_services.sh
CMD /app/start_services.sh

docker build -t gcr.io/${PROJECT_ID}/chatbot:v1 .

cd app/
# Start rasa server with nlu model
rasa run --model models --enable-api --cors "*" --debug \
         -p 5002

pip3 install uvloop


FROM ubuntu:18.04
ENTRYPOINT []

RUN apt-get update
RUN apt-get install -y python3 python3-pip
RUN apt-get install -y python3-venv && python3 -m venv ./venv
#RUN source ./venv/bin/activate
RUN pip3 install -y uvloop
RUN pip3 install -U pip
RUN pip3 install rasa==1.10.8
RUN pip3 install -y transformers 

ADD . /app/
RUN chmod +x /app/start_services.sh
CMD /app/start_services.sh


heroku login -i



import requests
url = 'http://hmm-za.herokuapp.com/webhooks/rest/webhook'
myobj = {
"message": "hi",
"sender": "rizal",
}

x = requests.post(url, json = myobj)
print(x.text)